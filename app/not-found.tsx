import SectionTitle from "@/components/global/SectionTitle";

function NotFound() {
  return (
    <main className="pt-36 sm:pt-28 min-h-[calc(100vh-34rem)]">
      <SectionTitle>Page could not be found.</SectionTitle>
    </main>
  );
}

export default NotFound;
